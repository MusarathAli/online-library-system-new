export interface Task {
    name: string;
    description: string;
    count: number;
    author: string;
}
export interface CompletedTask {
    name: string;
    description: string;
    count: number;
    author: string;
}
